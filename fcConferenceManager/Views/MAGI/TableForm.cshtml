@{
    ViewBag.Title = "TableForm";
}
<!DOCTYPE html>
<html>
<head></head>
<body onload="HideList()">
    <div class="mt-3">
        <div class="tab d-md-flex d-md-flex d-none tab">
            @*<button id="MAGIid">MAGI </button>


            @using (Html.BeginForm("EventsInfo", "MAGI", FormMethod.Post))
            {-->
                <button id="Homeid">Home </button>
                <!--<input type="submit" id="Homeid" value="Events" />
            }
            @using (Html.BeginForm("Index2", "InitialPage", FormMethod.Post))
            {-->
                <button id="Homeid">Home </button>
                <!--<input type="submit" id="Homeid" value="Home" />
            }*@


            @*<div id="tableid" class="listarea">

                <ul>
                    <li>
                        @using (Html.BeginForm("JoinMAGI", "MAGI", FormMethod.Post))
                        {
                            <input type="submit" class="Textbtn" id="btnCancel" value="Join MAGI" />
                        }
                    </li>
                    <li>
                        @using (Html.BeginForm("TermsOfUse", "MAGI", FormMethod.Post))
                        {
                            <input type="submit" class="Textbtn" id="btnCancel" value="Terms OF Use" />
                        }
                    </li>
                    <li>
                        @using (Html.BeginForm("ContactMAGI", "MAGI", FormMethod.Post))
                        {
                            <input type="submit" class="Textbtn" id="btnCancel" value="Contact MAGI" />
                        }
                    </li>
                    <li>
                        @using (Html.BeginForm("EventTermsandConditions", "MAGI", FormMethod.Post))
                        {
                            <input type="submit" class="Textbtn" id="btnCancel" value="Event Terms & Conditions" />
                        }
                    </li>

                </ul>
            </div>*@

        </div>


        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <h2>@ViewBag.PageTitle</h2>
                    @Html.Raw(@ViewBag.FormText)
                </div>
            </div>
        </div>
    </div>
    

    <script type="text/javascript">
        document.getElementById("MAGIid").onclick = function () {
            if (document.getElementById("tableid").style.display != "block") {
                document.getElementById("tableid").style.display = "block";
            } else {
                document.getElementById("tableid").style.display = "none";
            }
        }

        function HideList() {
            if (document.getElementById("tableid").style.display != "block") {
                document.getElementById("tableid").style.display = "none";
            }
        }
    </script>


</body>

</html>
